PORT=5000
# Example connection string (DO NOT put real credentials here)
# DATABASE_URL="postgresql://<role>:<password>@<HOST>/<DB_NAME>?sslmode=require"
DATABASE_URL=
JWT_SECRET=
# Optional: allowed origins for CORS (set these to your deployed frontends)
CUSTOMER_URL=
ADMIN_URL=
